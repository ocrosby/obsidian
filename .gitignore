# Obsidian workspace state (local UI layout, history)
.obsidian/workspace.json
.obsidian/workspace-mobile.json

# Cache files (search index, etc.)
.obsidian/graph.json
.obsidian/cache

# Optional: Ignore all plugins (if syncing separately or using plugin manager)
.obsidian/plugins/*

# EXCEPT: Keep plugin folder structure, ignore plugin binaries
!*.md
!.obsidian/plugins/
.obsidian/plugins/*/main.js
.obsidian/plugins/*/manifest.json

# OS-specific files
.DS_Store
Thumbs.db

# Node, Python, etc. project artifacts (if embedded)
node_modules/
venv/
dist/
