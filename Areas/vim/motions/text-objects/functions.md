# Text Objects for Functions (in languages like C, Python, etc.)

| Command | Description                     |
|---------|---------------------------------|
| `daf`   | Delete around function (via Treesitter) |
| `dif`   | Delete inside function          |

> Requires Treesitter support + plugins like `nvim-treesitter-textobjects`.
